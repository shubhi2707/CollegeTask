import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
  cityContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  weekContainer: {flex: 2, alignItems: 'center', justifyContent: 'center'},
});

export default styles;
